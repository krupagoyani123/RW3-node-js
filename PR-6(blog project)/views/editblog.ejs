<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Edit Page</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
        }

        .hero-section {
            background: linear-gradient(to right, rgba(41, 86, 132, 0.8), rgba(30, 67, 112, 0.8)), 
            url('assets/frontend/img/hero/Industries_hero.jpg') no-repeat center center / cover;
            height: 250px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
            text-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
        }

        .hero-section h1 {
            font-size: 2.5rem;
            font-weight: bold;
            animation: fadeInDown 1s ease-in-out;
        }

        .form-container {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.2);
            padding: 30px;
            margin-top: -100px;
            animation: fadeInUp 1s ease-in-out;
        }

        .form-container .form-control {
            border-radius: 8px;
        }

        .form-container textarea {
            resize: none;
        }

        .btn-primary {
            background-color: #295684;
            border-color: #295684;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #1e4370;
            border-color: #1e4370;
            box-shadow: 0 4px 10px rgba(30, 67, 112, 0.5);
        }

        .form-container img {
            max-width: 100%;
            border-radius: 8px;
            margin-top: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        .submit-btn {
            text-align: center;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <!-- Hero Section -->
    <div class="hero-section">
        <h1>Edit Your Blog</h1>
    </div>

    <!-- Form Section -->
    <div class="container">
        <div class="form-container mx-auto">
            <form action="/up" method="post" enctype="multipart/form-data">
                <!-- Hidden Field for ID -->
                <input type="hidden" name="editid" value="<%= single.id %>">

                <!-- Title Input -->
                <div class="mb-4">
                    <label for="title" class="form-label">Blog Title</label>
                    <input type="text" id="title" name="title" value="<%= single.title %>" class="form-control"
                        placeholder="Enter the blog title">
                </div>

                <!-- Description Input -->
                <div class="mb-4">
                    <label for="description" class="form-label">Blog Description</label>
                    <textarea id="description" name="desc" rows="5" class="form-control"
                        placeholder="Write a brief description"><%= single.desc %></textarea>
                </div>

                <!-- Image Upload -->
                <div class="mb-4">
                    <label for="image" class="form-label">Blog Image</label>
                    <input type="file" id="image" name="image" class="form-control">
                    <img src="<%= single.image %>" alt="Current blog image">
                </div>

                <!-- Submit Button -->
                <div class="submit-btn">
                    <button type="submit" class="btn btn-primary w-100 py-2">
                        <i class="fas fa-save"></i> Update Blog
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- JS Scripts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>

</html>
