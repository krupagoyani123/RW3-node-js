<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css"> <!-- Link to your CSS -->
    <title>Add/Edit User</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #74ebd5, #ACB6E5);
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 450px;
            padding: 30px;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }

        h2 {
            color: #6a11cb;
            margin-bottom: 25px;
            font-size: 2em;
            text-align: center;
            text-transform: uppercase;
        }

        .user-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        label {
            font-weight: bold;
            color: #333;
            text-transform: uppercase;
        }

        input[type="text"],
        input[type="date"] {
            padding: 15px;
            border: none;
            border-radius: 8px;
            background-color: #f4f4f4;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
            font-size: 1em;
            transition: all 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="date"]:focus {
            border: 2px solid #6a11cb;
            outline: none;
            box-shadow: 0 3px 15px rgba(106, 17, 203, 0.2);
        }

        .submit-button {
            padding: 15px;
            background-image: linear-gradient(to right, #6a11cb, #2575fc);
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.2em;
            font-weight: bold;
            text-transform: uppercase;
            transition: all 0.4s ease;
        }

        .submit-button:hover {
            background-image: linear-gradient(to right, #2575fc, #6a11cb);
            transform: translateY(-3px);
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>

<body>
    <div class="container">
        <h2><%= single ? 'Edit User' : 'Add User' %></h2>
        <form action="/recordInsert" method="post" class="user-form">
            <input type="hidden" name="editid" value="<%= single ? single.id : '' %>">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" value="<%= single ? single.name : '' %>" required>
            </div>
            <div class="form-group">
                <label for="phone">Phone:</label>
                <input type="text" name="phone" id="phone" value="<%= single ? single.phone : '' %>" required>
            </div>
            <div class="form-group">
                <label for="date">Date:</label>
                <input type="date" name="date" id="date" value="<%= single ? single.date : '' %>" required>
            </div>
            <button type="submit" class="submit-button">
                <%= single ? 'Update User' : 'Add User' %>
            </button>
        </form>
    </div>
</body>

</html>
